
import 'package:flutter/cupertino.dart';

class ConstPaddings {
  static const constScreenPadding = EdgeInsets.symmetric(horizontal: 16);
  static const constCardContentPadding = EdgeInsets.all(16);
}